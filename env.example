#
# Optionix Backend Environment Variables
# Based on configuration in code/backend/config.py
#

# -----------------------------------------------------------------------------
# 1. APPLICATION & SECURITY
# -----------------------------------------------------------------------------
# Environment setting (development, staging, production, testing)
ENVIRONMENT=production
# A secret key for signing tokens and other security-related needs (min 32 chars)
SECRET_KEY=
# A 32-character key for data encryption (CRITICAL)
ENCRYPTION_KEY=
# Comma-separated list of allowed CORS origins (e.g., http://localhost:3000,https://app.optionix.com)
CORS_ORIGINS=http://localhost:3000,https://optionix.com

# -----------------------------------------------------------------------------
# 2. DATABASE (PostgreSQL recommended)
# -----------------------------------------------------------------------------
# The connection string for the primary application database.
# Example: postgresql://user:password@host:port/dbname
DATABASE_URL=

# -----------------------------------------------------------------------------
# 3. REDIS
# -----------------------------------------------------------------------------
# Redis URL for general use
REDIS_URL=redis://localhost:6379/0
# Redis DB index for session management
REDIS_SESSION_DB=1
# Redis DB index for caching
REDIS_CACHE_DB=2
# Redis DB index for rate limiting
REDIS_RATE_LIMIT_DB=3

# -----------------------------------------------------------------------------
# 4. BLOCKCHAIN INTEGRATION
# -----------------------------------------------------------------------------
# RPC URL for the Ethereum network (e.g., Infura, Alchemy)
ETHEREUM_RPC_URL=https://mainnet.infura.io/v3/your-project-id
# The same as RPC URL, used as provider URL
ETHEREUM_PROVIDER_URL=https://mainnet.infura.io/v3/your-project-id
# Chain ID for the network (e.g., 1 for Ethereum Mainnet)
ETHEREUM_CHAIN_ID=1

# -----------------------------------------------------------------------------
# 5. MONITORING & LOGGING
# -----------------------------------------------------------------------------
# Sentry DSN for error tracking and performance monitoring.
SENTRY_DSN=
# Elastic APM Server URL
ELASTIC_APM_SERVER_URL=
# Elastic APM Secret Token
ELASTIC_APM_SECRET_TOKEN=
# Logging level (e.g., DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO
# Optional log file path
LOG_FILE=

# -----------------------------------------------------------------------------
# 6. EXTERNAL SERVICES
# -----------------------------------------------------------------------------
# Email Service (SMTP or API credentials would be needed here, not explicitly in config.py)
# SMS Service (Twilio or similar credentials would be needed here, not explicitly in config.py)

# -----------------------------------------------------------------------------
# 7. CLOUD STORAGE (for backups and artifacts)
# -----------------------------------------------------------------------------
# AWS Credentials (if using S3)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
# Azure Storage Credentials (if using Azure Blob Storage)
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
